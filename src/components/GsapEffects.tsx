import { useEffect } from "react";

export default function GsapEffects() {
  useEffect(() => {
    // GSAP effects can be added here later
    // For now, this is a placeholder
  }, []);

  return null;
}